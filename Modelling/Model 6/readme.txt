Finalising the models
come to the conclusion that the best way to train the model is to train it by using 
a common time range in which most of the features are present.

Therefore after EDA, trying different augmentation and transformation on the dataset 
before training the model.